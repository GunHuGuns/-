# OS配置中心 产品需求文档（PRD）

**文档版本**: V1.0  
**创建日期**: 2024年  
**产品名称**: OS配置中心管理后台

---

## 1. 产品概述

### 1.1 产品背景
OS配置中心是一个用于管理操作系统级别配置的后台管理系统，支持对系统模块和配置项进行统一管理、发布和控制。

### 1.2 产品目标
- 提供统一的系统模块管理能力
- 支持灵活的配置管理和发布机制
- 实现精细化的生效范围控制
- 支持定时发布功能

### 1.3 目标用户
- 系统运维人员
- 产品配置管理员
- 技术支持团队

---

## 2. 系统架构

### 2.1 功能模块划分

```
OS配置中心
├── 模块管理
│   ├── 模块列表
│   └── 新增模块
└── 配置管理
    ├── 配置列表
    └── 新建配置
        ├── Activity配置
        ├── 生效范围设置
        └── 定时发布
```

---

## 3. 模块管理

### 3.1 模块概述
模块是系统配置的基础单元，每个模块对应一个系统功能组件（如：系统设置、相机、电池等）。

### 3.2 模块列表页

![模块列表](/screenshots/module-list.png)

**功能说明**:
- 展示所有已创建的系统模块
- 支持多维度筛选查询
- 支持模块的上下架操作

**字段说明**:

| 字段名 | 说明 | 示例 |
|--------|------|------|
| 模块名称 | 模块的显示名称 | 系统设置模块 |
| 包名 | 模块的技术标识符 | com.os.settings |
| 新建时间 | 模块创建的时间 | 2024-01-15 00:00 |
| 品类 | 设备品类 | 手机 |
| 品牌 | 设备品牌 | 华为 |
| 型号 | 设备型号 | Mate 60 Pro |
| 国家 | 覆盖的国家/地区 | 中国、美国 |
| 状态 | 模块当前状态 | 已上架/已下架 |

**筛选条件**:
- 模块名称：支持模糊搜索
- 新建时间：支持日期范围选择
- 覆盖品类：下拉单选
- 覆盖品牌：下拉单选（依赖品类）
- 覆盖型号：下拉单选（依赖品牌）
- 覆盖国家：下拉单选

**操作按钮**:
- 新增模块：跳转至新增模块页面
- 重置：清空筛选条件
- 查询：执行筛选查询

### 3.3 新增模块页

![新增模块](/screenshots/module-new.png)

**功能说明**:
创建一个新的系统模块，需要填写基本信息并选择覆盖范围。

**字段说明**:

| 字段名 | 必填 | 说明 | 校验规则 |
|--------|------|------|----------|
| 名称 | 是 | 模块的显示名称 | 不能为空 |
| 包名 | 是 | 模块的技术标识符 | 格式：com.xxx.xxx |
| 所属品类 | 是 | 设备品类 | 必须选择 |
| 所属品牌 | 是 | 设备品牌 | 必须先选择品类 |
| 所属型号 | 是 | 设备型号 | 必须先选择品牌 |
| 覆盖国家 | 是 | 适用的国家/地区 | 支持多选 |

**业务规则**:
1. 品类、品牌、型号为级联关系，必须按顺序选择
2. 未选择上级时，下级选择器处于禁用状态
3. 包名格式需符合Android包名规范

---

## 4. 配置管理

### 4.1 配置概述
配置是针对具体模块的参数设置，包含Activity配置、生效范围和发布时间等信息。

### 4.2 配置列表页

![配置列表](/screenshots/config-new.png)

**功能说明**:
- 展示所有已创建的系统配置
- 支持配置的查看、编辑、发布操作

**字段说明**:

| 字段名 | 说明 |
|--------|------|
| 配置名称 | 配置的显示名称 |
| 模块ID | 关联的模块标识 |
| 生效范围 | 统一生效/定向生效(黑名单)/定向生效(白名单) |
| 状态 | 草稿/定时发布/已发布 |
| 创建时间 | 配置创建时间 |

**状态说明**:
- **草稿**: 配置已创建但未发布
- **定时发布**: 配置已设置定时发布时间，等待发布
- **已发布**: 配置已生效

### 4.3 新建配置页

![新建配置](/screenshots/config-new-form.png)

**功能说明**:
创建新的系统配置，包括基本信息、Activity配置、生效范围设置和定时发布。

#### 4.3.1 基本信息

| 字段名 | 必填 | 说明 |
|--------|------|------|
| 配置名称 | 是 | 配置的显示名称 |
| 选择模块 | 是 | 关联的系统模块 |

#### 4.3.2 Activity配置

![Activity配置对话框](/screenshots/activity-config-dialog.png)

**功能说明**:
配置模块中具体Activity的参数值。

**字段说明**:

| 字段名 | 必填 | 说明 |
|--------|------|------|
| Activity名称 | 是 | Activity的完整类名，如: com.example.MainActivity |
| Value类型 | 是 | 值的数据类型 |
| Value值 | 是 | 具体的配置值 |

**Value类型选项**:

![Value类型下拉](/screenshots/value-type-dropdown.png)

| 类型 | 说明 | 示例值 |
|------|------|--------|
| 单值-数字 | 单个数字值 | 100 |
| 单值-字符串 | 单个字符串值 | hello world |
| 对象-JSON | JSON对象 | {"enabled": true} |
| 数组-字符串 | 字符串数组 | ["a", "b", "c"] |
| 数组-数字 | 数字数组 | [1, 2, 3] |
| 数组-JSON | JSON对象数组 | [{"id": 1}, {"id": 2}] |
| 自定义 | 自定义格式 | 任意格式 |

**业务规则**:
1. 必须先选择Value类型，再输入Value值
2. 可添加多个Activity配置
3. 支持删除已添加的配置项

#### 4.3.3 生效范围设置

**功能说明**:
设置配置的生效范围，分为统一生效和定向生效两种方式，二者**互斥**。

**生效方式选择**:

| 生效方式 | 说明 |
|----------|------|
| 统一生效 | 按品类、品牌、型号、国家维度进行生效控制 |
| 定向生效 | 按设备列表（黑名单/白名单）进行生效控制 |

##### 4.3.3.1 统一生效

当选择「统一生效」时，可设置以下维度：

| 维度 | 说明 | 级联关系 |
|------|------|----------|
| 品类 | 设备品类筛选 | 无依赖 |
| 品牌 | 设备品牌筛选 | 依赖品类 |
| 型号 | 设备型号筛选 | 依赖品牌 |
| 国家 | 国家/地区筛选 | 无依赖 |

**业务规则**:
1. 品类、品牌、型号为级联关系
2. 未选择品类时，品牌选择器禁用
3. 未选择品牌时，型号选择器禁用
4. 支持多选

##### 4.3.3.2 定向生效

当选择「定向生效」时，需要配置：

| 字段 | 说明 |
|------|------|
| 名单类型 | 黑名单/白名单（二选一） |
| 设备列表 | 通过Excel上传设备ID列表 |

**名单类型说明**:
- **白名单**: 仅名单内的设备生效
- **黑名单**: 除名单内的设备外，其他设备均生效

**Excel上传规则**:
1. 支持 .xlsx、.xls 格式
2. 第一列为设备ID
3. 上传后显示已导入的设备数量

#### 4.3.4 定时发布

**功能说明**:
设置配置的定时发布时间。

| 字段 | 说明 |
|------|------|
| 启用定时发布 | 开关控制 |
| 发布日期 | 日期选择器 |
| 发布时间 | 时间选择器 |

**业务规则**:
1. 定时发布时间必须晚于当前时间
2. 到达发布时间后，系统自动将配置状态改为「已发布」

---

## 5. 数据字典

### 5.1 品类数据

| 品类ID | 品类名称 |
|--------|----------|
| phone | 手机 |
| tablet | 平板 |
| tv | 电视 |
| watch | 手表 |
| car | 车机 |

### 5.2 品牌数据

| 品牌ID | 品牌名称 | 所属品类 |
|--------|----------|----------|
| huawei | 华为 | phone, tablet, watch |
| xiaomi | 小米 | phone, tablet, tv, watch |
| oppo | OPPO | phone, tablet, watch |
| vivo | vivo | phone, tablet, watch |
| honor | 荣耀 | phone, tablet |

### 5.3 国家数据

| 国家代码 | 国家名称 |
|----------|----------|
| CN | 中国 |
| US | 美国 |
| JP | 日本 |
| DE | 德国 |
| FR | 法国 |
| GB | 英国 |
| IN | 印度 |
| BR | 巴西 |

### 5.4 状态定义

**模块状态**:
| 状态 | 说明 |
|------|------|
| online | 已上架 |
| offline | 已下架 |

**配置状态**:
| 状态 | 说明 |
|------|------|
| draft | 草稿 |
| scheduled | 定时发布 |
| published | 已发布 |

---

## 6. 交互规范

### 6.1 表单校验
- 必填字段使用红色星号(*)标识
- 提交时进行实时校验
- 校验失败时显示错误提示

### 6.2 级联选择
- 未选择上级时，下级选择器禁用并显示提示文字
- 选择上级后，下级选择器自动启用

### 6.3 状态提示
- 操作成功后显示绿色成功提示
- 操作失败后显示红色错误提示
- 支持Toast消息提示

### 6.4 对话框规范
- 对话框打开后，背景半透明遮罩
- 支持ESC键关闭对话框
- 支持点击遮罩层关闭对话框

---

## 7. 附录

### 7.1 术语表

| 术语 | 说明 |
|------|------|
| Activity | Android系统中的界面组件 |
| 包名 | Android应用的唯一标识符 |
| 生效范围 | 配置适用的设备范围 |
| 黑名单 | 排除列表，名单内设备不生效 |
| 白名单 | 包含列表，仅名单内设备生效 |

### 7.2 版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2024 | 初始版本 | - |
